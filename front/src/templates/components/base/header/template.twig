<header>
    {%include "../headerSocial/template.twig"%}
    <div class="container">
        <div class="header-content">
            <a href="#">
                <img src="/img/header-logo.png" alt="header-logo" class="header-logo">
            </a>
            <nav>
                {% for navItem in navItems %}
                    <a href="{{navItem.navLink}}" class="nav-item">{{navItem.navContent}}</a>
                {% endfor %}
            </nav>
        </div>
    </div>
</header>
